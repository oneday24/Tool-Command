http://start.spring.io/可以创建mvn spring boot项目

mvn dependency:tree  查看项目依赖
命令后面加-o offline

mvn compile 编译，会下载pom中的依赖
mvn spring-boot:run
mvn package 进行打包，会打包成一个可以直接运行的 JAR 文件
java -jar 命令就可以直接运行

cp /Users/zengjing/Desktop/settings.xml /Users/zengjing/.m2

spring boot 学习
1、装mvn，需要配置环境变量
2、将setting.xml放到.m2文件夹下
3、itellij idea用于开发
4、spring框架
5、spring boot框架和原理
6、die要点击旁边的mybatis-generator执行命令才会生成各种类和xml，spring boot，mybatis，mysql，mybatis generator，mapperscan

mvn mybatis-generator:generate



问题
mybatis generator mapper
